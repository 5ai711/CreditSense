# Keep Unix line endings for files that run inside Linux containers, even on a Windows checkout
# (a CRLF entrypoint script fails with "/bin/sh^M: bad interpreter" or "no such file or directory").
* text=auto
*.sh text eol=lf
Dockerfile text eol=lf
*.template text eol=lf
*.conf text eol=lf
